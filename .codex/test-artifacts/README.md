# Test Artifacts

Save verification screenshots and command logs under a cycle-specific directory:

```text
.codex/test-artifacts/<cycle-id>/
```

Artifact payloads are intentionally ignored by git. Reference the project-local
paths from cycle evidence instead of `/tmp` paths.
