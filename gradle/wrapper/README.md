# Wrapper note

The repository includes the standard `gradle-wrapper.jar` for Gradle 9.7.0.
The root `gradlew` can bootstrap the same Gradle distribution on Linux/macOS if
the JAR is absent.

For exact standard-wrapper behavior, restore the matching Gradle 9.7.0
`gradle-wrapper.jar` into this directory.
